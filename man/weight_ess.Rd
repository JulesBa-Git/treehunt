% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uniform_reference.R
\name{weight_ess}
\alias{weight_ess}
\title{Effective sample size based on importance weights}
\usage{
weight_ess(weights, log = FALSE)
}
\arguments{
\item{weights}{Non-negative finite weights, or log weights when \code{log = TRUE}.
Log weights may include \code{-Inf} (zero weight).}

\item{log}{Whether \code{weights} are on the natural-log scale.}
}
\value{
A numeric scalar between 1 and the number of supplied weights.
Returns \code{NA_real_} for an empty vector; all-zero weights are an error.
}
\description{
Measures weight concentration using \eqn{(\sum_i w_i)^2 / \sum_i w_i^2}.
Multiplying all weights by a common positive constant leaves this value
unchanged. Computation is rescaled to avoid overflow and underflow.
}
\details{
This diagnostic measures the concentration of importance weights.
It does not account for serial dependence in an MCMC trajectory and is not
a convergence diagnostic or an estimate of the number of independent draws.
}
\examples{
weight_ess(c(1, 1, 1))
weight_ess(c(1, 0.25, 0.25, 0.25, 0.25))
weight_ess(c(-1000, -1001), log = TRUE)
}
