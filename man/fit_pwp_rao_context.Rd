% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pwp_rao.R
\name{fit_pwp_rao_context}
\alias{fit_pwp_rao_context}
\alias{print.pwp_rao_context}
\title{Fit the reduced PWP model and prepare a Rao-score context}
\usage{
fit_pwp_rao_context(
  data,
  status_column,
  time_column = "gap_time",
  strata_column = "event_stratum_25",
  id_column = "id",
  covariates = c("age_start", "index_emergency", "year_start"),
  ties = "efron",
  positive_only = TRUE,
  min_covered_patients = 1L,
  min_covered_events = 1L
)

\method{print}{pwp_rao_context}(x, ...)
}
\arguments{
\item{data}{Interval-level data frame.}

\item{status_column}{Binary endpoint column.}

\item{time_column}{Gap-time column.}

\item{strata_column}{PWP event-order stratum.}

\item{id_column}{Patient identifier used for robust clustering.}

\item{covariates}{Character vector of reduced-model covariates.}

\item{ties}{Cox tied-time method. The optimized engine currently requires
`"efron"`.}

\item{positive_only}{If `TRUE`, the search fitness is `max(0, signed_z)`;
full signed statistics are still returned during detailed evaluation.}

\item{min_covered_patients}{Minimum number of distinct exposed patients for
a candidate to receive non-zero search fitness.}

\item{min_covered_events}{Minimum number of exposed endpoint events for a
candidate to receive non-zero search fitness.}

\item{x}{A `pwp_rao_context` to print.}

\item{...}{Additional print arguments (currently ignored).}
}
\value{
An object of class `pwp_rao_context`.
}
\description{
The reduced Cox model is fitted once. Its linear predictor, nuisance design
matrix and model-based nuisance variance are stored in a compact context used
by the optimized C++ score engine. Patient-clustered variance is retained in
the fitted model for reporting, while the high-throughput Rao statistic uses
the exact model-based efficient information.
}
